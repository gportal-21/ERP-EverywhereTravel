# API Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
websockets==13.1

# Async PostgreSQL
# Migraciones: NO se usa Alembic — el schema se aplica de forma idempotente en el
# arranque vía api/database.py::ensure_schema_compatibility() + infrastructure/postgres/init.sql
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10

# Redis
redis[hiredis]==5.2.1

# RabbitMQ
aio-pika==9.4.3

# AI / Multiagente
# Proveedor activo por defecto: Ollama local (LLM_PROVIDER=ollama), llamado directo
# por HTTP desde agents/swarms_compat.py (sin pasar por swarms/litellm).
# anthropic queda declarado como punto de extensión: si se configura
# LLM_PROVIDER=anthropic + ANTHROPIC_API_KEY, swarms/litellm rutea a Claude en vez
# de Ollama (agents/swarms_compat.py::Agent delega a swarms.Agent en ese caso).
anthropic==0.40.0
pgvector==0.3.6
# swarms y sus deps (langchain, litellm) se instalan en capa separada del Dockerfile
# con --no-deps para evitar que torch/transformers rompan el build (ver Dockerfile.api)

# Validación y schemas
pydantic==2.10.3
pydantic-settings==2.6.1
jsonschema==4.23.0

# Auth
PyJWT==2.8.0
passlib[bcrypt]==1.7.4

# Documentos
jinja2==3.1.4
boto3==1.35.78
xhtml2pdf==0.2.16
# svglib y reportlab se resuelven con versiones compatibles (pycairo se compila con build-essential)

# Observabilidad
prometheus-fastapi-instrumentator==7.0.0
structlog==24.4.0

# HTTP client
httpx==0.28.1

# Utilidades
python-dotenv==1.0.1
tenacity==9.0.0
aiofiles==24.1.0
orjson==3.10.12
toml==0.10.2
