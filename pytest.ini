[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: tests que requieren la API y servicios corriendo
    adversarial: tests de seguridad y edge cases extremos
    unit: tests unitarios sin dependencias externas
